<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fruitapp">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".FruitApp"
        android:theme="@style/Theme.FruitApp.NoActionBar">
        <activity
            android:name=".ui.fruitadd.AddActivity"
            android:exported="false" />
        <activity
            android:name=".ui.fruitdetails.DetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.fruitedit.EditActivity"
            android:exported="false" />
        <activity
            android:name=".ui.fruitlist.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>